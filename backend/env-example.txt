# ============================================================
#  BACKEND ENVIRONMENT VARIABLES
#  Copy this file to .env and fill in your real values.
#  Add each variable below to Vercel → Settings → Environment Variables
# ============================================================

# ----- Server -----
PORT=8000
NODE_ENV=production

# ----- MongoDB Atlas -----
# Get this from: https://cloud.mongodb.com → your cluster → Connect → Drivers
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/E-commerce?retryWrites=true&w=majority

# ----- JWT Authentication -----
# Use a long random string (min 32 chars). Generate one at: https://generate-secret.vercel.app/32
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRATION=3600
JWT_REFRESH_EXPIRATION=86400

# ----- Admin Credentials (used by frontend hardcoded login) -----
ADMIN_EMAIL=example@gmail.com
ADMIN_PASSWORD=0099
